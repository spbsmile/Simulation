namespace Simulation
{
    public class Settings
    {
        // second
        public const float Moving = 0.5f;
        public const float Recreation = 255 / 2;
        public const float FatigueatWork = 255 / 5;
        public const float Food = 255 / 1;
        public const float Starvation = 255 / 2;
        public const float Fatigue = 1;
    }
}
